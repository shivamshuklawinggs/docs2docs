# Nginx configuration for Next.js app deployed under /docks2doc
# Place this in your main nginx server block

location /docks2doc {
    # Proxy to Next.js server running on port 7003
    proxy_pass http://localhost:7003;
    
    # Forward headers
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Timeouts
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # Buffer settings
    proxy_buffering off;
    proxy_cache_bypass $http_upgrade;
}

# Optional: Serve static files directly for better performance
# location /docks2doc/_next/static {
#     alias /path/to/your/project/.next/static;
#     expires 365d;
#     add_header Cache-Control "public, immutable";
# }
